import styled from 'styled-components';
import Pagination from '@material-ui/lab/Pagination';

export const PaginList = styled(Pagination)`
  /* font-size: 10px; */
  color: red;
`;

// export const PaginItem = styled(PageItem)``;
